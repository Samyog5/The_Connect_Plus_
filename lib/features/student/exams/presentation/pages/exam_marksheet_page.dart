import 'package:flutter/material.dart';

class ExamMarksheetPage extends StatelessWidget {
  const ExamMarksheetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Exam Marksheet')),
      body: const Center(child: Text('Marksheet Content Coming Soon')),
    );
  }
}
