import 'package:flutter/material.dart';

class InternalMarksPage extends StatelessWidget {
  const InternalMarksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Internal Marks')),
      body: const Center(child: Text('Internal Marks Content Coming Soon')),
    );
  }
}
